apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/scrape: "true"
  name: k8s-top-exporter
  namespace: kube-system
spec:
  type: ClusterIP
  selector:
    app: k8s-top-exporter
  ports:
  - port: 9977
    targetPort: 9977
